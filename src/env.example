# Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=silex_dev
DB_USER=postgres
DB_PASSWORD=your_password_here
DB_SSL=false

# JWT
JWT_SECRET=change_me_in_production_min_32_chars
JWT_EXPIRES_IN=30d

# OTP — salt mixed into the hash, keep secret
OTP_SECRET=change_me_in_production_min_32_chars

# Environment
NODE_ENV=development
PORT=3000
